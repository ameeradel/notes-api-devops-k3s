apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: notes-api-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: notes-app
  namespaceSelector:
    matchNames:
      - default
  endpoints:
    - port: http
      path: /metrics
      interval: 5s